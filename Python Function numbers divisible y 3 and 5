def divisibilite(x): 
  if ((x % 5 == 0) and (x % 3 == 0)): 
   return ("Divisible par 5 & 3")
  elif x%3==0: 
   return 'Divisible par 3'
  elif x%5==0: 
   return  'Divisible par 5'
  else: 
    return ('{} is number is not divisible by 3 or 5'.format(x))
divisibilite(2)
